.idea
.vscode
upload
*.exe
*.exe~
test-build
*.db
build
*.db-journal
*.db-shm
*.db-wal
logs
data
/web/node_modules
# 注意：gitignore 不支持 "./" 前缀，原先的 "./.claude/settings.local.json"
# 实际从未生效。改为不带前缀的形式。
.claude/settings.local.json
.claude/worktrees/
.env
# .env.production / .env.local 这类变体也要挡住，但放行 .env.example
# —— 它是不含真实值的模板，需要受跟踪
.env.*
!.env.example
agents.md
graphify-out