# 排名功能发布前审查与数据库优化

用户已授权审查、修复并在验证后后端打 tag 推送、前端推送 GitHub。

## 范围

- 审查 `f5c9fa0` 后端、`c66d491` 前端的日聚合、缓存、日志清理、用量和发布行为。
- 空闲检查先读少量状态，仅有到期任务才获取写租约；移除 worker 对完整 JSON 的反复读取。
- 缓存同步在 SQL 中按版本过滤，未变化时不传输 payload。
- 日志清理改为有数量和时间预算的短事务批次，沿用可重试的删除数量响应；关闭榜单也保护未完成统计的日志。
- 检查原生 Claude 与 AWS/OpenAI 兼容路径的缓存 Token；修正遗漏。
- 用户补充当前仍在少量测试数据阶段，已授权加入统计索引：PostgreSQL 部分覆盖索引，SQLite 部分复合索引，MySQL 窄复合索引；不执行现有业务数据库的迁移或清理。

## 验证与发布

- 新增空闲数据库零写入、缓存版本、分批清理与关闭后恢复、统计查询索引计划的测试。
- 在隔离数据库构造代表性日志分布，观察查询计划和耗时；明确与生产规模的区别。
- 隔离检出执行完整 Go build/vet、相关包测试，前端构建与必要浏览器检查。
- 核对远端不存在新 tag、main 无分叉；后端创建 `v0.1.34` 并推送 main/tag，前端推送 main。核对远端提交及 CI 触发情况。
